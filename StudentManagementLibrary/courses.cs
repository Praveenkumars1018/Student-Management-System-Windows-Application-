namespace StudentManager
{ /// <summary>
/// The Type enum was added for showing the available Courses
/// </summary>
    public enum Courses
    {
        Mathematics,
        Science,
        English,
        History,
        Tamil
    }
}